# OffboardAI Backend

Render-ready Express API with JSON file persistence. Set `PORT` through Render; the server defaults to `10000` locally.

## Local

```bash
npm install
npm run dev
```

Health check: `http://localhost:10000/health`
